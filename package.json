{
  "name": "input",
  "version": "1.0.0",
  "private": true,
  "description": "Input monorepo",
  "homepage": "https://github.com/ricardocasares/input#readme",
  "bugs": {
    "url": "https://github.com/ricardocasares/input/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ricardocasares/input.git"
  },
  "author": "Ricardo Casares (https://analogic.al)",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "run-s docker:*",
    "stop": "docker-compose down",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:logs": "docker logs input_app_1 -f",
    "hasura": "hasura --admin-secret secret --project ./packages/hasura/",
    "console": "hasura console --admin-secret secret --project ./packages/hasura/",
    "format": "npx prettier --write **/*.{js}",
    "release": "npx semantic-release"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js}": [
      "npm run prettier",
      "git add"
    ]
  },
  "devDependencies": {
    "@types/node": "^12.0.7",
    "husky": "^1.3.1",
    "lint-staged": "^8.1.0",
    "npm-run-all": "^4.1.5"
  },
  "release": {
    "plugins": [
      "@semantic-release/github",
      "@semantic-release/release-notes-generator"
    ]
  }
}
