import { Font } from "./font";
import { Space } from "./space";
import { Color } from "./color";
import { Button } from "./button";

export type Theme = {
  font: Font;
  space: Space;
  color: Color;
  button: Button;
};
